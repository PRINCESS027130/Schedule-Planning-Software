@model IEnumerable<TimeTableManagement.Models.COURSE_ASSIGN_TBL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <a href="@Url.Action("Create", "COURSE_ASSIGN_TBL")" class="glyphicon glyphicon-pencil">Create New</a> 
</p>
<div class="table-agile-info">
    <table class="table">
        <tr>
            <th>
                COURSE NAME
            </th>
            <th>
              DEGREE NAME
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.COURSE_TBL.COURSE_NAME
                </td>
                <td>
                    @item.DEGREE_TBL.DEGREE_NAME
                </td>
                <td>
                    <a href="@Url.Action("Edit", "COURSE_ASSIGN_TBL", new { id=item.COURSE_ASSIGN_ID })" class="glyphicon glyphicon-edit"></a> |
                    <a href="@Url.Action("Details", "COURSE_ASSIGN_TBL", new { id=item.COURSE_ASSIGN_ID })" class="glyphicon glyphicon-book"></a>  |
                    <a href="@Url.Action("Delete", "COURSE_ASSIGN_TBL", new { id=item.COURSE_ASSIGN_ID })" class="glyphicon glyphicon-trash"></a>
                </td>
            </tr>
        }

    </table>
    </div>